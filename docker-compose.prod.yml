services:
  backend:
    build: ./backend
    ports:
      - "${PUERTO_API}:3001"
    env_file: ./backend/.env
  frontend:
    build: ./frontend
    ports:
      - "${PUERTO_WEB}:3000"
    env_file: ./frontend/.env